# LogMapping 0.2.2-beta — 2026-09-18

PC 사용 문제 수정 로컬 빌드입니다. NAS·휴대폰 동작은 검증 범위에서 제외했습니다. 원본 작업 폴더에 적용했으며 GitHub 릴리스는 발행하지 않았습니다.

## 수정 내용

- 다른 카탈로그를 열거나 목록에서 하드를 제거할 때 공유 DB의 다른 스캔 기록이 삭제되지 않도록 변경했습니다.
- 재스캔은 새 기록으로 작성하고 이전 기록을 보존하며, 같은 경로의 색상 태그를 옮깁니다. 일반 폴더의 읽기 오류·취소는 스캔 실패로 처리합니다.
- 손상 DB를 자동 삭제하지 않습니다. .hcat은 임시 파일에 쓰고 교체하며 직전 파일을 .bak으로 보존합니다. 실패 시 오류·미저장 상태를 유지합니다.
- 저장을 직렬화하고 종료 시 저장 완료를 기다립니다. 다른 이름 저장 중 추가 변경의 미저장 표시를 유지합니다.
- .hcat의 DB 식별자·드라이브 참조를 검증합니다. 검색·CSV·HTML 내보내기는 현재 카탈로그에 포함된 드라이브로 제한됩니다.
- 백업 버튼은 DB와 .hcat을 같은 백업 폴더에 보관합니다. 백업 파일을 임시 파일로 생성한 다음 교체합니다.
- 경로 검색, 색상·분류 필터, 용량 정렬, 결과 화면의 보기 크기를 수정했습니다. 최대 표시 개수를 안내하고 오래된 비동기 검색 응답의 화면 덮어쓰기를 방지합니다.
- 검색 결과에 태그를 지정할 때 결과의 실제 드라이브를 갱신합니다. 경로 복사는 드라이브 루트를 포함합니다.
- HTML 뷰어는 모든 드라이브를 검색한 뒤 관련 결과를 제한 표시합니다. 특수 폴더명과 줄바꿈 경로를 처리합니다.
- 점으로 시작하는 파일을 포함하고 디렉터리 연결 지점은 재귀 탐색하지 않습니다. Windows 루트 용량은 실제 드라이브 값으로 표시합니다.
- APFS의 점 포함 폴더 분류, 볼륨 이름·UUID 필드, 다중 볼륨 선택·취소 처리를 수정했습니다. HFS 식별도 디스크 번호 대신 볼륨 정보를 사용합니다.
- 고장 하드 등록의 초기화, 글자 크기 CSS, 명도, 키보드 버튼·레이블·모달 포커스·Escape 처리를 보완했습니다.
- 같은 크기로 변경된 HTML도 갱신하도록 리소스 해시를 확인합니다. CSV·HTML 저장창은 해당 확장자를 사용합니다.
- 스캔 성공 뒤 압축 때문에 다음 스캔이나 종료가 지연되지 않도록 HTML 캐시는 내보낼 때 만듭니다. 최초 내보내기는 시간이 더 걸릴 수 있습니다.

## 데이터 이동·복원

실행파일만 교체하면 기존 실행 위치의 data 폴더를 사용합니다. 다른 위치에 있는 실행파일은 그 위치의 data 폴더를 사용하므로 카탈로그가 비어 보일 수 있습니다.

다른 PC로 이동하려면 앱을 종료한 뒤 실행파일, data 폴더 전체, 외부 위치에 별도로 저장한 .hcat을 함께 복사하세요. .hcat 하나만으로 파일 목록이 복원되지는 않습니다.

백업 세트는 data/backups 아래에 catalog.db와 catalog.hcat으로 생성됩니다. 복원 시 앱을 종료하고 기존 data 폴더를 별도 보관한 뒤, 깨끗한 실행 위치의 data/catalog.db에 백업 DB를 복사하고 같은 세트의 catalog.hcat을 여세요. 기존 DB의 WAL/SHM과 다른 백업 DB를 섞지 마세요. 이전 버전으로 되돌릴 때도 그 버전과 짝을 이루는 데이터 백업을 사용하세요.

## 검증

- C# 회귀 시험 22개 통과: DB 범위, 마이그레이션, 백업, 원자적 저장, 읽기 실패·취소, 합성 APFS 등.
- JavaScript 회귀 시험 16개 통과: 저장 실패·경합, 교차 드라이브 태그, 검색 필터·정렬·응답 경합, 뷰어 등.
- 브라우저 모의 네이티브 연결에서 PC 800×500 / 1100×700 화면, 보기 변경, 키보드 실행, 등록 양식, Escape와 오류 로그를 확인했습니다.
- Windows x64 .NET 8 자체 포함 단일 실행파일 publish 성공. WebView2 Runtime은 별도 필요합니다.
- 재실행 방법은 tests/README.md를 참고하세요.

## 남은 제한과 검증 범위

- 실제 WPF/WebView2 실행창, 실제 외장 APFS/HFS 매체, 분리·재연결·UAC·Windows 파일 선택창의 종단 간 검증은 하지 못했습니다. APFS 수정은 합성 자료 시험이며 모든 매체 호환성을 보장하지 않습니다.
- 암호화된 Mac 볼륨과 기존 비지원 APFS 구조의 지원 범위는 확장하지 않았습니다.
- 실사용 수백만 행 DB의 성능 시험은 이번 수정본에서 수행하지 않았습니다. 첫 실행의 경로 색인 재구축과 최초 HTML 내보내기는 시간이 걸릴 수 있습니다.
- 이전 스캔을 보존하므로 DB 크기가 증가합니다. 참조 관계를 확인하는 기록 정리 UI는 아직 없습니다. 대용량 DB는 자동 백업이 생략될 수 있으므로 백업 버튼을 사용하세요.
- .hcat의 DB 식별자가 없는 구버전 파일은 참조 ID와 경로로 검증합니다. 다른 DB에 같은 ID·경로가 존재하는 경우까지 완전히 판별할 수는 없습니다.
- 내보낸 HTML은 압축 자료 전체를 메모리로 읽는 구조입니다. 검색 누락은 수정했지만 대규모 자료의 메모리 상한·폴더 목록 가상화는 남아 있습니다.
- 화면은 기존 디자인을 유지하며 결함과 가독성을 보완했습니다. 전면 재디자인이나 전체 접근성 적합성 인증은 아닙니다.
- NAS·휴대폰·실제 원격 공유 연결은 이번 검증 대상이 아닙니다. 공용 HTML 뷰어 코드는 PC 검색 결함 수정 범위에서 변경되었습니다.

# LogMapping 0.2.3-beta — 실물 장비 전 검증

2026-09-18 수정·빌드, 2026-09-19 사전 릴리스입니다. **미완성 / 작업 중**이며 실물 검증은 남아 있습니다. NAS·휴대폰은 제외했습니다. 이전 [0.2.2 변경 기록](CHANGELOG-0.2.2.md)에 이어 아래 보완을 적용했습니다.

## 추가 수정

- 종료 자동백업에서 실제 실패를 숨기던 처리를 제거해 저장 오류를 전달합니다. 대용량 정책상 생략과 실패를 구분합니다.
- 대용량 판단에 WAL 크기도 포함하고, 스캔 후 크기 제한을 넘으면 화면 안내를 갱신합니다.
- DB 스키마 초기화 실패 시 연결 핸들을 해제해 복구 파일 작업이 막히지 않게 했습니다.
- 카탈로그를 읽는 중 다른 카탈로그를 열거나 변경하는 충돌을 차단했습니다.
- 종료 저장 중 새 변경 작업을 차단하고, 종료가 취소되면 조작을 다시 허용합니다.
- 중복 드라이브 번호·DB 참조와 잘못된 태그·메타데이터 형식을 거부하고 날짜를 HTML 텍스트로 처리합니다.
- HTML 뷰어에서 검색어 변경·검색 지우기·드라이브 전환 시 이전 검색을 중단합니다. 오래된 진행 표시가 새 화면을 덮지 않게 했습니다.

## 검증 범위

자동 시험 총 **67개 통과**: C# 회귀 24, JS 회귀 22, 대용량·장애 16, 실제 임시 파일 스캔 3, 대용량 백업 정책 1, 구형 색인 전환 1. 별도로 WebView2 설치 및 포함 리소스 검사도 통과했습니다.

- **40개의 논리 드라이브, 드라이브당 16만 개, 총 640만 개 합성 파일 메타데이터**입니다. 실제 디스크 40개를 연결하거나 파일 640만 개를 생성한 시험은 아닙니다.
- 실제 파일 시스템 스캔은 **3만 개 임시 파일**로 별도 수행했습니다. 순환 junction, 점 파일, 빈 파일, 260자 초과 한글 경로, 스캔 중 폴더 소멸을 확인했습니다.
- 강제 종료 뒤 WAL 복구, 잠긴 백업 파일, 읽기 전용 .hcat, SQLite FULL 오류를 시험했습니다. 디스크를 실제로 가득 채우지는 않았습니다.
- 최종 HTML을 브라우저에서 열어 40번째 하드의 `asset-6399999.jpg`, 전체 **6400000개 일치 / 500개 표시**, 검색 취소·드라이브 전환을 확인했습니다.
- PC UI는 모의 네이티브 응답으로 5만 개 표시 제한, 20개 행만 렌더링하는 가상 목록, 오류 표시, 등록 양식, 키보드와 포커스 이동을 확인했습니다.

## 측정값

| 작업 | 시간 |
|---|---:|
| 640만 개 목록 생성 | 498.629초 |
| 마지막 드라이브의 특정 파일 검색 | 0.139초 |
| 없는 검색어 | 0.002초 |
| 전체 일치 검색 + 용량 정렬 + 상위 50,001개 조회 | 29.082초 |
| 두 글자 검색 | 13.668초 |
| 8개 동시 작업 × 5회 검색·태그 쓰기 | 0.517초 |
| 전체 CSV 내보내기 + 행 수 확인 | 45.644초 |
| 최초 HTML 뷰어 내보내기 | 44.582초 |
| 캐시 사용 HTML 내보내기 | 0.085초 |
| 백업·새 폴더 복원·목록/태그 확인 | 24.337초 |
| DB 재열기 + 특정 파일 검색 | 0.044초 |
| 전수 무결성 검사 | 170.433초 |
| 구형 이름 색인 → 경로 색인 재구축 | 154.662초 |

- 주 시험 DB: 2.69 GiB / HTML: 21.30 MiB.
- 주 부하 시험 프로세스 최대 작업 집합: 819.2 MiB. 브라우저나 전체 WPF 앱의 메모리 수치는 아닙니다.
- 색인 전환은 합성 DB를 복사해 구형 name-only FTS 구조로 만든 뒤 재구축했습니다. 원본 파일 행과 40개 드라이브, 마지막 경로 검색을 확인했습니다.
- 한 PC의 합성 자료 단일 실행 측정입니다. OS 캐시·동시 부하·실제 파일명과 경로에 따라 달라집니다. 물리 드라이브 읽기 속도를 나타내지 않습니다.

## 남은 제약

- **640만 개 전체 정렬 검색 약 29초, 두 글자 검색 약 14초**의 지연은 남아 있습니다. 오류나 누락은 없었지만 즉시 응답을 보장하지 않습니다. 색인 재구축과 최초 내보내기도 대기 시간이 있습니다.
- **실제 WPF 실행창, UAC, Windows 파일 선택창, 실제 화면 배율(DPI)의 종단 간 조작은 미검증**입니다. 현재 도구에서 네이티브 앱 제어가 비활성화되어 수행하지 못했습니다. 브라우저 시험·빌드·리소스 검사는 이를 대체하지 않습니다.
- 물리 외장 하드, APFS/HFS 장비, 연결 해제·재연결, 암호화 매체, NAS·휴대폰은 검증 대상이 아닙니다.
- 이전 스캔 기록을 보존하므로 DB는 커집니다. 참조를 안전하게 확인하는 기록 정리 UI는 아직 없습니다.

## 실행·데이터 보존

Windows x64 자체 포함 .NET 8 단일 EXE입니다. WebView2 Runtime은 필요하며 현재 PC의 설치 버전은 153.0.4234.32로 확인했습니다.

기존 실행 위치의 EXE만 교체하면 그 위치의 data 폴더를 계속 사용합니다. 다른 위치에서 EXE를 실행하면 해당 위치의 data 폴더를 사용합니다. 이동·복원은 앱을 종료하고 .hcat과 data 폴더를 함께 보관하세요. 백업 세트를 복원할 때 기존 데이터는 별도로 보존하고 깨끗한 실행 위치의 data/catalog.db와 같은 세트의 .hcat을 사용하세요.

사용자 DB·.hcat은 이번 시험에 사용하거나 수정하지 않았습니다. 재현 명령은 [tests/README.md](tests/README.md)에 있습니다. 배포: [v0.2.3-beta](https://github.com/VULCAN-HUB/LogMapping/releases/tag/v0.2.3-beta).

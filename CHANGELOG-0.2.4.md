# LogMapping 0.2.4-beta — 0.2.3 코드 리뷰 결함 수정

2026-09-26 사전 릴리스입니다. **미완성 / 작업 중**이며 실물 장비 검증은 남아 있습니다. [0.2.3 변경 기록](CHANGELOG-0.2.3.md)에 이어, 0.2.3 코드 리뷰에서 찾은 결함 7건을 고쳤습니다.

## 수정

- **종료 시 자동 백업이 실패하면 창을 닫을 수 없던 문제.** 백업 파일이 잠겼거나 디스크가 가득 차면 종료가 매번 취소되어 강제 종료 외에는 방법이 없었습니다. 이제 실패 사유를 보여 주고 "백업 없이 종료할까요?"를 묻습니다. 어느 쪽이든 DB는 정상적으로 닫힙니다.
- **Mac(APFS) 드라이브 재스캔이 거부되던 문제.** 저장된 볼륨 번호가 컨테이너 구성 변화로 어긋나면, 같은 볼륨(UUID 일치)이 연결돼 있어도 재스캔이 막혔습니다. 이제 볼륨 UUID로 먼저 찾습니다.
- **HTML 뷰어 크기.** 한글 경로가 `\uXXXX`로 저장되어 압축 전 데이터가 약 2배로 커지던 문제를 고쳤습니다. `<`, `&` 같은 HTML 민감 문자는 계속 이스케이프합니다. 기존에 만든 뷰어 캐시도 그대로 쓸 수 있습니다.
- **스캔 중 사라진 파일 하나 때문에 스캔 전체가 실패하던 문제.** 임시 파일이나 백신 격리처럼 목록 조회 직후 사라진 항목은 건너뛰고, 제외 개수를 스캔 결과 안내에 표시합니다. 드라이브 연결이 끊겨 스캔 시작 경로 자체가 사라지면 지금처럼 실패로 처리해 기존 목록을 보존합니다.
- **스캔 중단 버튼이 드물게 반응하지 않던 경합**을 제거했습니다.
- **검색 결과를 빠르게 스크롤할 때 끊기던 문제.** 접근성 속성 보정이 매 프레임 문서 전체를 다시 훑던 것을, 새로 그려진 행만 처리하도록 바꿨습니다.
- **저장 실패 원인이 "접근 거부"로 가려지던 문제.** 임시 파일 정리 실패가 원래 오류를 덮지 않습니다(카탈로그 저장·DB 백업).

## 검증 범위

- Release 빌드 경고 0개, 오류 0개.
- 자동 시험: C# 회귀 25개, JS 회귀 22개, 실제 파일 스캔 4개 통과. 뷰어 데이터 한글·`</script>` 경로 시험과 스캔 중 폴더/루트 소멸 시험을 새로 넣었고, 새 시험이 수정 전 코드에서 실패하는 것을 확인했습니다.
- 대용량·장애 시험(40개 논리 드라이브, 640만 건 합성 메타데이터): 16개 통과(2026-09-26 재실행). 특정 파일 검색 0.37초, 두 글자 검색 7.2초, 용량 정렬 검색 31.7초, CSV 전체 23.2초, HTML 최초 11.0초 / 캐시 0.1초, 무결성 검사 145초. 합성 경로가 ASCII라 한글 크기 절감은 이 수치에 드러나지 않습니다.
- **실제 WPF 창 조작(종료 백업 실패 대화상자, 스크롤 체감)과 실물 외장 디스크·APFS/HFS 검증은 하지 않았습니다.** NAS·휴대폰은 제외했습니다.
